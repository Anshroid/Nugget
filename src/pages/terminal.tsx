import Typography from "@mui/material/Typography";
import Head from "next/head";

export default function Terminal({}) {
    return (
        <>
            <Head>
                <title>Nugget Terminal</title>
            </Head>
            <Typography>Terminal</Typography>
        </>
    )
};

